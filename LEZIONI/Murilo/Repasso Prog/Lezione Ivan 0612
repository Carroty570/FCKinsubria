//Quelle che ci sono i simboli "!!" sono le possibile domande dell'esame seccondo il tutor

- Construtore è un metodo con lo stesso nome della classe

#Conceto di overloading !!

Chiamare il metodo con lo stesos nome, ma fa una funzione diversa

#public, private e protected# !!

public, tutti vedono

private, solo la classe vede

protected, solo la classe e tutto che la extend



---classe astrate---

Una classe che non crea oggeti, quindi una classe che on si può stanziare

Evita la creazione dei metodi

Diferenza tra classe atratta e interfacia !!

La diferenza è: L'interfacia si può creare pìu di una 

##Eccezioni## !!

SOno gli errori che vengono generati durante il programma

###Eccezione###

--Throw-- 

Literalmente lancia una eccezione !!

-----------------------------------------

class ExezioneCreata extends Exception !!

per creare una eccezione 